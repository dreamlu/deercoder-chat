# prod mode/docker mode

# consul
consul.address = consul:8500
consul.scheme = http

# redis config
redis.host = redis:6379
redis.password =
redis.database = 0
redis.maxOpenConn = 0
redis.maxIdleConn = 0