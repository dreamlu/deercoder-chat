app:
  # database
  db:
    host: tcp(mysql:3306)
    name: deercoder-chat
    password: dreamlu
    user: dreamlu
  # redis
  redis:
    host: redis:6379
  # consul
  consul:
    address: consul:8500
  log:
    level: error